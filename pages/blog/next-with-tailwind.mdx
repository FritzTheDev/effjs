import { Header } from "../../lib/Header";
import { CodeBlock } from "../../lib/CodeBlock";

<Header />
<div className="container mx-auto">
  
  <CodeBlock language="javascript">
    {`
  /*
   * This blog post is written in a mix of react & markdown, 
   * using a library called "MDX"
   * 
   * It lets me just jot down some markdown but also sprinkle in React-ified
   * components like this code block.
   * Neat, right?
   */
  const greeting = "Hello World!";
  const HelloWorld = () => <div>{greeting}</div>
`}
  </CodeBlock>
</div>
